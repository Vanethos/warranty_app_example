name: warranty_app_example
description: Warranty App

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  firebase_core: 0.3.1+1
  firebase_auth: ^0.8.4
  google_sign_in: ^4.0.1+3
  cloud_firestore: ^0.9.11

  cupertino_icons: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true
  assets:
  - assets/